<section class="user-profile">
<h1>Your orders</h1>

<!-- product = Product.find(op.product_id)  -->
<% if @orders.first.status != "open"  %>
<table>
  <tr>
    <th>Order</th>
    <th>Status</th>
    <th>Total</th>
  </tr>
  <tbody>
    <tr>
      <% @orders.each do |order| %>
      <% unless order.status == "open" %> <!-- Only show orders that were checkedout already (not show the order in the cart) -->
      <td> <strong><%= link_to  "Order ##{order.id}", order_path(order.id)  %><br /></strong></td>
      <td><%= order.status.capitalize %></td>
      <td><%= format_price(order.total) %></td>
      <% end %>
    </tr>
    <% end %>
  </tbody>
</table>
<% else %>
  <h4>You have no orders yet.</h4>
  <p><%= link_to("Continue Shopping", new_product_path, :class => "button") %>
<% end %>
<section>
